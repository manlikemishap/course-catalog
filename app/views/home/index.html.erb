<%= form_tag controller: "home", action: "index" do %>
  <%= search_field_tag :search, params[:search] %>
  <%= submit_tag "Search" %>
<% end %>

<% if @results %>
  <% @results.each do |course| %>
    <%= course.title %> <br>
  <% end %>
<% end %>